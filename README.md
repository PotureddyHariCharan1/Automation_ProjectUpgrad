# Automation_ProjectUpgrad
#
# Automation to run Ubentu and if not installed maake it installed. 
# We are assigned to write an automation bash script named ‘automation.sh’ to perform the following subtasks.
# --> update of the package details and the package list at the start of the scrip
# --> Ensure that the apache2 service is enabled.
# --> Create a tar archive of apache2 access logs and error logs
# --> The name of tar archive should have following format:  <your _name>-httpd-logs-<timestamp>.tar.
# --> run the AWS CLI command and copy the archive to the s3 bucket.
# --> Initialise a Git repository with the name ‘Automation_Project’ and update the readme.md file which explains the project.
